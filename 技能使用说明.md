# 业务流程图生成技能 · 使用说明

给同事看：怎么装、怎么口述出图、编辑器一期能改什么。Agent 指令在同目录 `SKILL.md`。

本包和「原型批注」一样，放在 `D:\网厅需求演示系统\` 下，拷走即可用，机器只要已装 **Node.js** 和 Cursor。

## 1. 装到 Cursor

把整个「业务流程图生成技能」文件夹复制一份，**改名为** `biz-flow-gen`，放到下面任一位置：


| 范围     | 路径                                          |
| ------ | ------------------------------------------- |
| 本机所有工程 | `C:\Users\<你>\.cursor\skills\biz-flow-gen\` |
| 只给某个仓库 | `<对方仓库>\.cursor\skills\biz-flow-gen\`       |


拷完后文件夹里应能看到：

```
biz-flow-gen/
  SKILL.md
  技能使用说明.md
  editor/
  scripts/serve.mjs
```

新开一轮 Cursor 对话（或 Reload Window），对 Agent 说「按业务流程图技能，根据我这段流程出图」。

本机发布目录（源包，可再拷给别人）：

`D:\网厅需求演示系统\业务流程图生成技能`

## 2. 对话里给齐三样

1. **口述流程**，或贴一张流程图截图（可以两个都给）
2. **落盘目录**（你工程里的任意文件夹，建议绝对路径）
3. 业务名称（可省略，默认用目录名）

示例：

> 用业务流程图技能。流程：客户进介绍页 → 选销户原因 → 要挽留就进预约，否则准入校验 → 提交审核 → 结束。落盘到 `D:\demo\一站式销户`，标题「一站式销户」。

Agent 会写好 `业务流程.md`，并给你一条本地编辑链接。

## 3. 产出是什么

同一目录两个文件：

- `业务流程.md`：mermaid，给开发阅读，Markdown 预览（`Ctrl+Shift+V`）也能看
- `biz-flow.json`：画布坐标；第一次打开编辑器再保存后才会有。保存后两个文件都在你指定的目录里，可挂进 md 需求文档或高保真说明。

编辑器地址形如：

`http://127.0.0.1:8790/?dir=D:\demo\一站式销户`

先等 Agent 把本地服务拉起来，再点链接。不要用 soc-prototype 的 `pnpm dev`，也不要用 8788 批注端口。

### 示例效果图

口述或贴图出稿后，本地画布大致是这样：左侧图形库，中间角色泳道 + 判定/批注，顶栏可加竖列/横行、缩放、复位视图与保存（Ctrl+S 会同步回写 mermaid）。

<!-- 图1：业务流程图编辑器效果（一站式销户示意） -->
![图1：业务流程图编辑器效果示意](assets/editor-preview.png)

*图中红框：左侧图形库、顶栏「保存」、左下操作提示。交叉泳道（竖列角色 + 横行系统）可在画布里继续叠加。*

## 4. 一期编辑器能做什么


| 能力        | 怎么用                                             |
| --------- | ----------------------------------------------- |
| 图形        | 左侧：开始/结束、处理、判定、批注                               |
| 泳道        | 竖列≈角色、横行≈系统，可叠加；左侧/顶栏/画布「+」新增                   |
| 改名 / 删除泳道 | 双击标题改名；点选后 Delete                               |
| 调列宽 / 行高  | 拖泳道分割线                                          |
| 节点        | 拖拽；方向键按网格挪；Shift 多选                             |
| 连线        | 四边圆点拉线；中段拖折弯；双击改文字                              |
| 快捷键       | Ctrl+Z / Y 撤销重做；C / X / V 复制剪切粘贴；S 保存；Delete 删除 |
| 复位视图      | 只重置平移和缩放，不改图                                    |
| 保存        | 同时回写 md（一维 mermaid）和 json（位置）                   |


交叉矩阵（又有角色列又有系统行）请在画布里加横行。mermaid 里仍然主要是竖向角色泳道，方便开发读。

## 5. 自己起编辑器（不经过对话）

目录里已有 `业务流程.md` 时：

```bash
node "D:\网厅需求演示系统\业务流程图生成技能\scripts\serve.mjs" --dir "D:\demo\一站式销户" --port 8790
```

浏览器打开命令行打印的地址。端口被占用就换 `--port 8791`。

## 6. 常见问题

**点开链接是空白 / 连不上**  
先看对话或终端里有没有 `[biz-flow] editor http://127.0.0.1:...`。没有就按上一节手动起服务。

**端口占用**  
换端口：`--port 8791`。不要去抢原型批注的 8788。

**同事没有本仓库也能用吗**  
能。拷这个技能文件夹即可，不要要求对方打开 soc-prototype。

**改了画布，Markdown 预览没更新**  
在编辑器里再按一次保存（Ctrl+S）。

**以后编辑器加了功能**  
只改本包里的 `editor/`，并同步改 `SKILL.md` 与本说明里的「一期编辑器」表。

## 7. 不要指望技能去做的事

- 不提供 MCP，不单独做截图 OCR 服务（截图由对话里的模型看）
- 不把交叉泳道画进 mermaid 正文
- 不绑定开工单 / caseId / 本仓库开发服务器

